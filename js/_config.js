EVE = (function config(module) {
    'use strict';

    module.config = {
        egMax: 2.125,
        egMin: 0.025,
        glideMax: 0.165,
        lfo2Max: 139,
        masterFreq: 440
    };

    return module;
}(EVE));
